{
  "name": "brand-archetype-quiz",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered brand archetype quiz funnel for e-commerce brands",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "server": "nodemon server/server.js",
    "client": "cd client && npm run dev",
    "dev": "cd client && npm run dev",
    "prebuild": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": [
    "brand",
    "quiz",
    "archetype",
    "ai",
    "react",
    "node"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@netlify/functions": "^2.0.0",
    "openai": "^4.20.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 